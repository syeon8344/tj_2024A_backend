데이터 자료 저장
-이유 : 다시 쓰려고
-리터럴 객체를 저장
-자료형 변수명 = 리터럴 혹은 객체 혹은 메소드반환값

//1.
ex) 회원정보를 이름과 나이 저장
- String name = "유재석"
- int age = 40;
string int 타입의 자료 달라서 배열저장x

회원 m1 = new 회원();
m1.name = "유재석";
m1=age = 40;
회원 타입의 자료 가 같아서 배열저장o

//2. 100명을 저장 해주세요.
name 변수와 age 변수 x 100생성
변수가 많아지면 관리가 힘들다.
ex ) 붕어빵 만들기 위해 붕어빵틀을 만드는 것처럼
회원1명을 만들기 위해 회원틀을 만들자 ==> class(클래스) 만들자 [설계측면에서 생각하기]
자바에서의 배열은 같은 타입의 자료끼리 저장 가능

//1. 배열저장방법
String[]names = new String[100];
int[] ages = new int[100];
//2. 배열저장방법
회원[] members = new회원[100];
//3. 생성자 : 생성(만드는것) 자(함수) , (자바면접 20%)
//클래스를 만드는 이유 : 객체를 만들기 위해(다양함)
//객체를 왜 만들죠 ? : 객체는 속성/고유성질 과 메소드/행위 로 구성된 하나의 실체
//왜 실체가 필요한 이유 ? : 현실세계를 디지털화 시키기위해
//왜 디지털 화 시키죠 ? : 컴퓨터는 피로도가 없고 전기만 있으면 사람대신에 업무가 가능하니까.
//예) 쿠팡 물류 공장에서 내가 제품을 포장을 하는 역할
// - 사람은 피로도,급여
// - 포장하는 행위/메소드 를 객체화 (기계화) > 사람대신에 컴퓨터가 포장하는 행위/메소드
// - 포장을 어떻게 하는지? : 알고리즘/순서도
// - 박스 선택 > 박스 접고 > 제품 넣기 > 박스 테이핑 > 점검 (하나의 로직)
    //객체를 생성할때 사용하는 연산자 : 생성자

==========================================================================
//2. 생성자
//클래스내 생성자를 선언하지않으면/없으면 자동으로 기본 생성자 하나 제공함
//-생성자가 1개 이상이면 자동생성이 안됨.
 //생성자명은 클래스명과 동일한데 ? 식별이 깨진다.
//-생성자명이 동일해도 식별가능하게 해주는 기능 : 오버로딩(오버로드)
//-매게변수의 개수 , 위치 , 타입ㅇ에 따라 식별 단) 변수명은 아니다.

//this란 ? : 매개변수와 멤버변수의 변수명이 동일할때
// 구별하기 위해 현재객체 멤버변수를 칭(언급)할때
===================================================================================
오늘 정리 할것 (java에서)
(원초적인 이유 정리 (왜?)사용 8살도 이해할수있게)

1. 타입이 무엇인지? (여러가지 자료형)
- 대답 : 값을 저장하기 위한 여러가지 자료형 (기본자료형과 참조자료형)(int String char 등)
1-1 타입을 언제사용하는지
- 내가 원하는 데이터를 저장할때 사용

2. 클래스가 무엇인지? (객체의 설계도)
-객체의 설계도 : 서로 다른 타입(자료형들)들의 집합
-객체의 대한 세부정보
2-1 클래스를 왜 언제 사용해야 하는지?
-객체를 만들기/생성하기 위해 ,
-반복되는 데이터를 보다쉽게 정리하기위해 - 하나의 상자
-유지보수 편리성 , 일체화

3. 객체란 무엇인지? (속성/행위를 구현하는 틀)
- 속성/고유성질 과 메소드/행위로 구성된 하나의 실체/틀
3-1 객체를 왜 사용해야 하는지?
-속성과 메소드로 구성된 하나의 실체를 만들기위해/만들어야할때
-실체를 만들어 현실에 있는 모든것을 디지털화 하기위해

4. 생성자가 무엇인지? (초기화)
- 멤버변수를 초기화 하는것
- 객체를 호출할때 생성되는 함수
4-1 생성자를 왜 사용하고 사용해야하는지 , 언제 사용하는지
- 선언한 멤버변수안에 있는 값을 초기화 하고 사용하기위해 기본 값은 무엇이 들어있는지 알수없기때문에

5. 멤버변수가 무엇인지? (클래스에서 선언된 변수)
- 클래스 안쪽 / 영역에서 선언된 변수
5-1 멤버변수는 왜 사용하는지 , 사용해야하는지 , 언제사용 하는지?
- 클래스 안에서 변수를 선언할때
왜? 변수 선언 - 변수안에 값을 넣음으로써 기능을 활성화 시키기위해
(변수를 선언함으로써 기능을 활성화 시키기위해)

6. 함수(Method)란? (객체 끼리의 상호작용)
- 클래스의 기능을 구현하고 움직이게하는
- 특정 기능을 하는 코드를 묶어서 나타낸것 (상호작용)
6-1 언제 사용하는지 안에 무엇이있는지 //void int
- 필요한 기능을 실행 시켜야 할 때

7. 매개변수란 ? (전달받은 값)
함수내에서 사용할 변수를 호출시 전달받을 값들
7-1 매개변수란 언제 사용 해야 되는지?
함수안에서 변수를 호출하여 변수 안의 값을 전달할때

8. 반환타입이란? (특정기능(메소드) 실행후 값의 자료형)
메소드 실행 후 값의 타입(자료형)을 의미
반환값이 있을때는 : 자료형 사용
없을때는 : void사용
8-1 리턴이란? (함수를 호출한 곳으로 값 반환)
값을 반환해주는 기능으로 종료시 결과를 함수를 호출한 곳으로 값 반환해주는것
8-2 리턴을 언제 사용 해야 되는지?
값을 반환해서 되돌려 줘야할때
필요없으면 void쓰니까
